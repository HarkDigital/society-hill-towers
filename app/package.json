{
  "name": "philly3d-app",
  "version": "1.0.0",
  "private": true,
  "description": "Philly3D as an iOS and Android app: the Capacitor wrapper around the built page in ../3d-model",
  "scripts": {
    "copy-web": "node scripts/copy-web.mjs",
    "sync": "node scripts/copy-web.mjs && npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
